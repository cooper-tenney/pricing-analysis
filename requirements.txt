pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.5
# openai>=1.0  # optional: for --summarize-columns with OPENAI_API_KEY

# Web UI
fastapi>=0.100
uvicorn>=0.22
python-multipart>=0.0.6
jinja2>=3.1

# Bayesian optimization
optuna>=3.0

# Optional: PDF table extraction (app works with CSV without these)
# pip install pdfplumber  # simpler, recommended
# pip install 'camelot-py[cv]'  # alternative

# Optional: LightGBM for lgbm_optional model
# pip install lightgbm
